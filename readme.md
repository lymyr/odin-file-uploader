# todo: revise later
## details
- used `isAuth.js` and `isOwner.js` middleware to protect routes/files
- deletion of folder with folders/files within it
- cloudinary storage
- moving of files/folders,
- share a folder to allow public viewing (only shows the files within it, not the folders inside it)
- validations and error handling by using an error catcher per route (file and folder router)

## dev setup
- `touch .env`
- `check env.example and put in required stuffs`
- `node app.js`