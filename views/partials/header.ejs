<header>
    <h1><a href="/">Odin File Uploader</a></h1>
    <% if (locals.user) {%>
        <div>
            <p> <%= user.username %> </p>
            <form method="post" action="/logout">
                <button>Log out</button>
            </form>
        </div>
    <% }%>
</header>